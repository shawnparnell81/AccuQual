name: AccuQual CodeQL config
# Tests contain deliberate fake credentials and hostile inputs; scanning them produces noise, not findings.
paths-ignore:
  - "**/test/**"
  - "**/*.test.ts"
  - "**/dist/**"
  - "**/node_modules/**"
queries:
  - uses: security-extended
