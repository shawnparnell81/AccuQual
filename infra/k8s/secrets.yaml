# Template only — do NOT commit real values.
# In practice these are populated via Azure Key Vault + the CSI Secret Store
# driver (see infra/terraform/keyvault) or `kubectl create secret` in CI,
# never checked into git.
apiVersion: v1
kind: Secret
metadata:
  name: accuqual-secrets
  namespace: accuqual
type: Opaque
stringData:
  DATABASE_URL: "postgres://REPLACE_ME@REPLACE_ME:5432/accuqual"
  JWT_ACCESS_SECRET: "REPLACE_ME"
  JWT_REFRESH_SECRET: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  AZURE_STORAGE_CONNECTION_STRING: "REPLACE_ME"
